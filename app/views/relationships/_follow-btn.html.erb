  <% if current_user.following?(user) %>
    <%= link_to "unfollow", unfollow_path(user), method: :post, remote:true, class: "btn" %>
  <% else %>
    <%= link_to "follow", follow_path(@user),method: :post, remote:true, class: "btn" %>
  <% end %>